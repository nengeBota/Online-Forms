describe("server", () => {
	test.todo(`given that the inputs are valid
  then the submit function should return the correct response`);

	test.todo(`given that the inputs are invalid
  then the submit function should return the correct response`);

	test.todo(`create the various situations where input might be invalid`);

	test.todo(
		`break up the values in the constants file into their own files, for easy finding`
	);
});

// passing valid inputs results in the correct functions being called, and the correct outputu being received

// passing various missing inputs results in the expected output being received

// testing of any helper functions
